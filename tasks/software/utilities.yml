---
# This file contains configuration steps for various utilities

- name: software | utilities | install package
  ansible.builtin.apt:
    state: latest
    name: 
      - at
      - curl
      - glances
      - htop
      - tmux
      - whois
      - logrotate
